version: '3'
services:
  bot:
    build: .
    command: python bot.py
  worker:
    build: .
    command: python worker.py
  web:
    build: .
    command: uvicorn web:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
